{
  "name": "5hr",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run packages/*/src apps/*/src",
    "lint": "eslint . --ext .ts",
    "format": "prettier --write .",
    "dev": "pnpm --filter @5hr/cli run dev"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "prettier": "^3.2.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
